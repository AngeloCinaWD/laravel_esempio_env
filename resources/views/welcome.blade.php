{{--@foreach($app_name_env as $env)
    <div>{{$env}}</div>
@endforeach--}}

{{--{{$app_name_env}}--}}
@dump($app_name_env)
@dump($database)
{{--@dump($bamba)

@foreach($bamba as $bimba)
    <ul>
        <li>
            {{$bimba['id']}}
        </li>
    </ul>
@endforeach--}}
